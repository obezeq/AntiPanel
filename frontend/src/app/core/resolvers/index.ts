/**
 * Route Resolvers
 *
 * Functional resolvers for preloading data before route activation.
 * All resolvers follow Angular 21+ ResolveFn pattern.
 */

export { orderResolver } from './order.resolver';
